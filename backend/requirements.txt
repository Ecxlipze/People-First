# People First API — Django 6.1 / DRF
Django>=6.1,<6.2
djangorestframework>=3.16
djangorestframework-simplejwt>=5.5
django-cors-headers>=4.7
python-decouple>=3.8
# ImageField on every content model requires Pillow.
Pillow>=11.0
# settings.py uses the postgresql engine.
psycopg[binary]>=3.2
